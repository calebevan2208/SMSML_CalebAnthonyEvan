name: SMSML_CalebAnthony

entry_points:
  main:
    parameters:
      epochs: {type: int, default: 50}
      batch_size: {type: int, default: 32}
      learning_rate: {type: float, default: 0.001}
      data_path: {type: str, default: "../../Eksperimen_SML_CalebAnthonyEvan/preprocessing/churn_preprocessing/clean_data.csv"}
      artifacts_dir: {type: str, default: "artifacts"}
    command: "python modelling.py --epochs {epochs} --batch_size {batch_size} --learning_rate {learning_rate} --data_path '{data_path}' --artifacts_dir '{artifacts_dir}'"
